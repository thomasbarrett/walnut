walnut is serving model {{ .Values.model | quote }} as {{ include "walnut.fullname" . }}.

Reach the OpenAI-compatible API:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if eq .Values.service.type "ClusterIP" }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "walnut.fullname" . }} 8000:{{ .Values.service.port }}
  curl http://127.0.0.1:8000/v1/models
{{- else }}
  kubectl -n {{ .Release.Namespace }} get svc {{ include "walnut.fullname" . }}
{{- end }}
