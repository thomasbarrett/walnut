apiVersion: v1
kind: Service
metadata:
  name: {{ include "walnut.fullname" . }}
  labels:
    {{- include "walnut.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "walnut.selectorLabels" . | nindent 4 }}
